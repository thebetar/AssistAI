# 2023-11-28

**Round robin** is a scheduling method for processes where each process gets a set amount of time and if this time is exceeded the proces is put at the back of the queue and the next process can use the processor.

**ADC** analog to digital converter

**SoC** is a system on a chip and is a processor that can be used to process input.